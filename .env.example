# Keycloak configuration for the frontend.
# Copied to frontend/.env.local and filled in; injected into the build by
# `scripts/set-env.mjs` (see frontend/package.json prebuild hook).

KEYCLOAK_URL=
KEYCLOAK_REALM=Rumoo
KEYCLOAK_CLIENT_ID=